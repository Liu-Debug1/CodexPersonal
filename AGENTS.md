# Codex 扩展资产管理

## 适用范围

- 本目录统一管理 Codex 使用的 Skill、Plugin、MCP 和 CLI 资产。
- `LIU-Skill-repository/` 只保存已认可的个人源资产；`Skill待测库/` 只保存尚未入库的候选资产；`项目包/<项目名>/` 保存项目选用的可部署版本。
- 本规则当前只定义 Codex 的运行和部署方式。不得将 Claude Code 的运行目录或配置作为 Codex 部署目标。

## 资产生命周期

1. 新资产先进入 `Skill待测库/`，登记来源、版本、权限、依赖、风险和真实试用结果。
2. 候选资产通过静态审计和实际验收后，才可复制到 `LIU-Skill-repository/`；同时更新个人库的 `README.md` 和 `Skill分类索引.md`。
3. 某项目需要资产时，从个人库或已确认的项目资产建立 `项目包/<项目名>/` 副本，并在 `manifest.json` 中登记绝对项目路径、来源、版本和哈希。
4. 项目运行目录只接受项目包部署，不得直接手工修改。项目包修改是否回写个人库，必须先询问用户。

## Skill 项目包部署

- `项目包/<项目名>/skills/` 是 `<项目绝对路径>/.codex/skills/` 的唯一可编辑来源。
- 修改项目包中的任一 Skill 后，默认将改动同步到 manifest 指向的真实项目：连续执行 `PlanSkills`、`DeploySkills` 和 `VerifySkills`。只有用户明确要求“先不需要同步更新”或等效表述时，才只更新项目包而不部署。
- 执行 Skill 部署前，必须先运行 `scripts/Sync-CodexProjectPackage.ps1` 的 `PlanSkills` 预检。
- `DeploySkills` 仅删除 manifest 指向项目的 `.codex/skills/` 目录，再完整复制项目包的 `skills/`；不得删除 `.codex/agents`、`.codex/config.toml`、`.codex/settings.local.json` 或项目业务文件。
- 部署后必须运行 `VerifySkills`，确认目录、文件和 SHA-256 与项目包一致，并更新项目包和项目包总览的最后更新日期。

## 其他资产

- MCP 项目包保存受管配置片段，只更新项目配置中脚本管理的 MCP 区块；凭据只记录环境变量名，不写入项目包。
- Plugin 在 Codex 的插件目录中按其实际安装机制管理。项目包记录所需插件、版本和验收状态，不把插件目录伪装成可直接复制的项目运行目录。
- CLI 项目包记录所需命令、版本、用途和本机验证状态；CLI 的安装位置依照其工具要求管理。
- 禁止用符号链接、目录联接或硬链接把待测库、个人库或其他项目包暴露为项目运行目录。

## 执行接口

- 用户可以直接要求测试候选资产、入库资产、创建或更新项目包、部署项目 Skill、验证项目包。
- 运行 `Sync-CodexProjectPackage.ps1` 前读取项目包的 `manifest.json`，以其中的项目绝对路径为准；不能仅凭目录名称推断目标。
- 脚本预检失败时不得删除目标目录。当前部署策略不保存项目运行目录快照，因此通过预检后的删除和完整复制必须连续执行。
